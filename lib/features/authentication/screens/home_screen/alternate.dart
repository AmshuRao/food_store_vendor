import 'package:flutter/material.dart';

class Alternate extends StatelessWidget {
  const Alternate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("heelo terhere");
  }
}